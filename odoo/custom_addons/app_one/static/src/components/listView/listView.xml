<?xml version="1.0" encoding="UTF-8"?>

<templates xml:space="preserve">
    <t t-name="app_one.ListView">
        <h1>Properties List</h1>

        <button class="btn btn-primary m-1 p-1" type="button" t-on-click="toggleCreateForm">
            <t t-if = "state.showCreateForm">Close</t>
            <t t-else ="">New</t>
        </button>

        <t t-if="state.showCreateForm">
            <FormView onRecordCreated="onRecordCreated" onCancel="toggleCreateForm.bind(this)"/>

        </t>

        <div class="o_list_view table-responsive">
            <table class="o_list_table table table-striped table-hover table-sm">
                <thead>
                    <tr>
                        <th class="o_list_column_header" >Name</th>
                        <th class="o_list_column_header">Postcode</th>
                        <th class="o_list_column_header">Date Availability</th>
                        <th class="o_list_column_header">Action</th>

                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="state.records" t-as="record" t-key="record.id">
                        <tr>
                            <td><t t-esc="record.name"/></td>
                            <td><t t-esc="record.postcode"/></td>
                            <td><t t-esc="record.date_availability"/></td>
                            <td>
                                <button class="btn btn-danger" type="button" t-on-click="deleteRecord.bind(this, record.id)">
                                    Delete
                                </button>
                            </td>
                        </tr>
                    </t>
                    
                </tbody>
            </table>
        </div>
    </t>
</templates>